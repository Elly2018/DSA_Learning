#include <stdio.h>


/*
 * This will just print all the argument user has input in.
 * Just a training process.
 */
int main(int argc, char* argv[]){
  for(int i = 0; i < argc; i++){
    printf("%s\n", argv[i]);
  }
  return 0;
}
